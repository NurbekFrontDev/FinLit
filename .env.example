# Копия этого файла — реальный .env (он в .gitignore и не попадает в GitHub)
# Значения берутся в Supabase: Settings -> API
VITE_SUPABASE_URL=your-project-url
VITE_SUPABASE_ANON_KEY=your-publishable-key
